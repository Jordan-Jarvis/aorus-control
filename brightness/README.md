# Brightness mapping candidates

There is intentionally no mapping candidate here yet. The machine's actual
Fn brightness event and scan value have not been captured, so adding a hwdb
rule would be an unsafe guess.

Use `../tools/brightness-capture.sh` and review
`../docs/brightness-debug.md` before creating a narrowly matched candidate.
